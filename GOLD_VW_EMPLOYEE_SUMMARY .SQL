CREATE OR REPLACE VIEW GOLD.VW_EMPLOYEE_SUMMARY  AS
SELECT 
 DEPARTMENT,
    COUNT(*) AS TOTAL_EMPLOYEES,
    SUM(SALARY) AS TOTAL_SALARY,
    AVG(SALARY) AS AVG_SALARY,
    MIN(SALARY) AS MIN_SALARY,
    MAX(SALARY) AS MAX_SALARY
FROM SILVER.EMPLOYEE_SILVER
GROUP  BY DEPARTMENT