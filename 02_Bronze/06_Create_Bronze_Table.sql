CREATE OR REPLACE TABLE EMPLOYEE_RAW
(
    EMPLOYEE_ID      INT,
    EMPLOYEE_NAME    STRING,
    GENDER           STRING,
    DEPARTMENT       STRING,
    SALARY           NUMBER(10,2),
    HIRE_DATE        DATE,
    EMAIL            STRING,
    PHONE            STRING
);
